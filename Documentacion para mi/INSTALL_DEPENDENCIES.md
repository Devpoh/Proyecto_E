# 📦 Instalación de Dependencias para Funcionalidades Avanzadas

## Instalar todas las dependencias necesarias:

```bash
cd frontend/electro_isla

# Chart.js para gráficos interactivos
npm install chart.js react-chartjs-2

# jsPDF para exportación a PDF
npm install jspdf jspdf-autotable

# xlsx para exportación a Excel
npm install xlsx

# Tipos de TypeScript
npm install --save-dev @types/jspdf
```

## Dependencias instaladas:

1. **chart.js** + **react-chartjs-2**: Gráficos interactivos
2. **jspdf** + **jspdf-autotable**: Exportación a PDF
3. **xlsx**: Exportación a Excel

## Verificar instalación:

```bash
npm list chart.js react-chartjs-2 jspdf xlsx
```

## Uso:

Después de instalar, las páginas de Estadísticas, Pedidos y funciones de exportación estarán completamente funcionales.
